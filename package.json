{
  "name": "vm-stats",
  "description": "Aggregate stats from the Node VM",
  "repository": "nearform/vm-stats",
  "version": "1.0.0",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps"
  },
  "dependencies": {
    "loopbench": "^1.2.0",
    "pidusage": "^1.1.6"
  },
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "prettier-bytes": "^1.0.4",
    "standard": "^10.0.2",
    "tape": "^4.8.0"
  },
  "keywords": [
    "vm",
    "stats"
  ],
  "license": "MIT"
}
